
// Define TypeScript interface for service `g1`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface G1Base {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface G1 extends G1Base {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
